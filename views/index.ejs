<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Welcome</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        /* General Body Style */
        body {
            font-family: 'Arial', sans-serif;
            text-align: center;
            margin: 0;
            padding: 20px;
            background-color: #f8f8f8; /* Light gray background */
        }

        /* Heading Style */
        h1 {
            color: #d4af37; /* Gold color for headers */
            font-size: 2.5em; /* Larger font size for headers */
            margin-bottom: 20px; /* Space below headers */
        }

        /* Image Style */
        .welcome-image {
            max-width: 80%; /* Limit the width of the image */
            height: auto; /* Maintain aspect ratio */
            margin: 20px 0; /* Margin for spacing */
            display: block; /* Center the image */
            margin-left: auto; /* Center horizontally */
            margin-right: auto; /* Center horizontally */
        }

        /* Link Style */
        a {
            text-decoration: none;
            color: #d4af37; /* Gold color for links */
            font-weight: bold;
            font-size: 1.2em; /* Slightly larger font size for links */
            transition: color 0.3s;
            display: inline-block; /* Make link a block to center */
            margin-top: 20px; /* Margin above the link */
            padding: 10px 20px; /* Padding for better click area */
            background-color: #fff; /* White background for button */
            border-radius: 5px; /* Rounded corners */
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1); /* Slight shadow for depth */
        }

        a:hover {
            color: #b8860b; /* Darker gold on hover */
            background-color: #f0f0f0; /* Light gray background on hover */
        }
    </style>
</head>
<body>
    <h1>Welcome to E-Commerce Site</h1>
    <img src="/images/Screenshot 2024-09-29 124021.png" alt="Welcome Image" class="welcome-image"> <!-- Centered Image -->
    <a href="/create-account">Get Started</a> <!-- Centered Link -->
</body>
</html>
